import {
  STAGGER_CHILDREN,
  TRANSITION_DURATION,
  VARIANTS,
} from "./animation-config";

export const config = {
  animation: {
    TRANSITION_DURATION,
    STAGGER_CHILDREN,
    VARIANTS,
  },
};
